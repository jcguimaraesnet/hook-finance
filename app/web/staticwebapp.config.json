{
  "platform": {
    "apiRuntime": "node:20"
  },
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/api/*",
      "/assets/*",
      "/canvaskit/*",
      "/icons/*",
      "/*.{png,svg,ico,webmanifest,js,css,wasm,json,map,otf,ttf,woff,woff2}"
    ]
  },
  "globalHeaders": {
    "Cache-Control": "no-cache"
  },
  "routes": [
    { "route": "/api/*", "allowedRoles": ["anonymous"] }
  ]
}
